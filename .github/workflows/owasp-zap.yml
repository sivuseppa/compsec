name: Run Zap on Pull Request

on:
  pull_request:
    branches: [ "main" ]

jobs:
  run-zap-on-pull-request:
    runs-on: ubuntu-latest
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: 'https://compsec.sivuseppa.dev'
